{
    "name": "polr",
    "description": "Get a poll going!",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "nuxt --hostname 192.168.1.57",
        "build": "nuxt build",
        "start": "nuxt start",
        "export": "nuxt export",
        "serve": "nuxt serve",
        "heroku-postbuild": "npm run build",
        "deploy": "git push heroku master"
    },
    "dependencies": {
        "@nuxtjs/apollo": "^4.0.1-rc.1",
        "@nuxtjs/now-builder": "^0.17.3",
        "lodash.debounce": "^4.0.8",
        "nuxt": "^2.13.0"
    },
    "devDependencies": {
        "@nuxtjs/color-mode": "^1.0.2"
    }
}
